# Because shortcuts/aliases for executables are sometimes
# unavailable via SSH, we need to specify the location when we connect.
# require:
#   - wp-cli/pre-ssh.php

# Aliases to other WordPress installs (e.g. `wp @staging rewrite flush`)
# An alias can include 'user', 'url', 'path', 'ssh', or 'http'
@dev:
  ssh: serverpilot@ssh.madebycaliper.com/srv/users/serverpilot/apps/wp-deploy/public
# @production
#   ssh: wpcli@wp-cli.org:2222
#   user: wpcli
#   path: /srv/www/wp-cli.org